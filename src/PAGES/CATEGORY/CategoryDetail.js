import React from "react";

const CategoryDetail = (props) => {
	console.log("category details props", props);
	return (
		<>
			<h1> Category Detail </h1>
		</>
	);
};

export default CategoryDetail;
